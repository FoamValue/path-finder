# PathFinder 文件管理系统 — 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名称 | PathFinder（寓意：寻找系统路径） |
| 文档版本 | v1.0.0 |
| 发布日期 | 2026-08-30 |
| 产品定位 | 个人/小型企业内容文件管理系统 |
| 目标形态 | 单组织私有部署（Web），不支持 SaaS 多租户 |

---

## 1. 背景与问题定义

### 1.1 业务背景

个人或小型企业在日常办公中会产生大量文件资产：合同、报表、设计稿、视频素材、技术文档等。文件散落在本地磁盘、网盘、IM 聊天记录中，面临以下痛点：

- **散乱无章**：文件分散在个人电脑和聊天软件中，缺乏统一的存储位置。
- **权限失控**：重要文件被无关人员访问，或离职员工带走资产。
- **不可追溯**：文件被覆盖、误删后无法找回，操作行为无审计记录。
- **跨设备不便**：无法在办公室、家中、移动端随时访问需要的文件。

### 1.2 问题定义

PathFinder 定位为一个**自部署、面向单一组织**的轻量文件管理系统，核心解决"文件放哪、谁能看、如何找"三个问题。系统名称寓意帮助用户在文件系统中"找到正确的路径"。

### 1.3 目标

| 目标 | 描述 | 衡量指标（北极星） |
|---|---|---|
| 统一存储 | 将组织内文件资产集中管理 | 文件统一入库率 100% |
| 权限可控 | 数据权限按组织架构隔离 | 越权访问事件 = 0 |
| 高效检索 | 快速定位所需文件 | 平均检索耗时 < 3s |
| 安全合规 | 操作全程可审计、防丢失 | 审计日志完整率 100% |

### 1.4 非目标（v1.0.0 明确不做）

- 不做 SaaS 多租户、不做组织间数据共享。
- 不做在线预览、在线编辑（保留为 v1.2 规划）。
- 不做全文检索、OCR、AI 智能分类（保留为 v1.3 规划）。
- 不做移动端原生 App（Web 响应式即可）。

---

## 2. 用户与角色

### 2.1 角色定义

| 角色 | 编码 | 说明 | 数量规模 |
|---|---|---|---|
| 系统管理员 | `ADMIN` | 组织超级管理员，管理用户、部门、系统配置 | 1~2 人 |
| 部门管理员 | `DEPT_ADMIN` | 部门负责人，管理本部门文件与成员 | 每部门 1 人 |
| 普通员工 | `USER` | 文件的上传、下载、查看、编辑权限内文件 | 主要群体 |
| 访客（只读） | `VIEWER` | 仅可浏览/下载被授权的文件 | 少量外部协作 |

### 2.2 组织架构与数据权限模型

- 单组织模型，通过**部门树（组织架构）**实现数据隔离。
- 文件归属三类范围：
  1. **个人空间（`PERSONAL`）**：仅本人可见。
  2. **部门空间（`DEPT`）**：本部门及下级部门成员可见。
  3. **公共空间（`PUBLIC`）**：全组织可见。
- 权限判定规则（可见性继承）：
  - 用户默认可见：自己的个人文件 + 所在部门（含上级部门授权）文件 + 公共文件。
  - 部门管理员可见本部门及全部下级部门文件。
  - 系统管理员可见全部文件。
- **v1.0.0 仅实现"可见性"级数据权限**（谁能看到），不做细粒度"操作级"权限（谁能删/改），操作级权限由文件所有者与角色约束。

---

## 3. 用户故事

> 用户故事采用格式：`作为 <角色>，我希望 <功能>，以便 <价值>`。优先级：P0（必须）/ P1（重要）/ P2（可选）。

### 3.1 登录与鉴权（U1 ~ U9）

| ID | 优先级 | 用户故事 |
|---|---|---|
| U1 | P0 | 作为系统用户，我希望使用用户名 + 密码登录系统，以便安全地访问我的文件 |
| U2 | P0 | 作为系统用户，我希望登录时密码经加密传输，以便防止密码在传输过程中被窃取 |
| U3 | P1 | 作为系统用户，我希望登录时输入图片验证码，以便防止自动化程序暴力破解我的账号 |
| U4 | P0 | 作为系统用户，我希望连续输错密码 5 次后账号被临时锁定 10 分钟，以便防止他人暴力破解 |
| U5 | P1 | 作为系统用户，我希望同一账号在新设备登录时旧会话被踢出，以便防止账号被他人冒用 |
| U6 | P0 | 作为系统用户，我希望系统在登录失败时给出明确提示，以便我知道密码是否正确 |
| U7 | P0 | 作为普通用户，我希望默认密码登录后必须修改初始密码，以便保护我的账户安全 |
| U8 | P0 | 作为系统用户，我希望会话超时后自动退出，以便防止他人在我离开后继续使用我的账户 |
| U9 | P1 | 作为系统用户，我希望主动退出登录，以便在公共电脑上保护我的文件不被他人查看 |

### 3.2 用户与组织管理（U10 ~ U13）

| ID | 优先级 | 用户故事 |
|---|---|---|
| U10 | P0 | 作为系统管理员，我希望管理组织架构（部门的新增/编辑/删除），以便文件能按部门归属 |
| U11 | P0 | 作为系统管理员，我希望管理用户账号（新增/编辑/停用/重置密码），以便控制谁能访问系统 |
| U12 | P1 | 作为系统管理员，我希望为部门指定部门管理员，以便让部门管理员分担本部门的管理职责 |
| U13 | P1 | 作为部门管理员，我希望查看和管理本部门成员，以便维护部门的文件权限边界 |

### 3.3 文件上传（U14 ~ U18）

| ID | 优先级 | 用户故事 |
|---|---|---|
| U14 | P0 | 作为普通用户，我希望支持单文件与批量文件上传，以便将多个文件一次性入库 |
| U15 | P0 | 作为普通用户，我希望上传时可选择存放空间（个人/部门/公共），以便控制文件可见范围 |
| U16 | P1 | 作为普通用户，我希望系统在上传失败时提示失败原因并支持重试，以便保证文件完整入库 |
| U17 | P1 | 作为普通用户，我希望系统对超大文件提供上传进度反馈，以便我了解上传状态 |
| U18 | P1 | 作为普通用户，我希望系统限制单文件大小上限并对超限文件进行拦截，以便防止磁盘被恶意占满 |

### 3.4 文件下载（U19 ~ U22）

| ID | 优先级 | 用户故事 |
|---|---|---|
| U19 | P0 | 作为普通用户，我希望下载自己有权限查看的文件，以便在本地使用 |
| U20 | P1 | 作为普通用户，我希望支持批量打包下载（ZIP），以便一次性获取多个相关文件 |
| U21 | P1 | 作为普通用户，我希望下载时能保留原始文件名，以便我无需手动重命名 |
| U22 | P2 | 作为 VIEWER 访客，我希望仅能下载被授权的文件且不能上传，以便实现最小权限协作 |

### 3.5 文件管理与检索（U23 ~ U28）

| ID | 优先级 | 用户故事 |
|---|---|---|
| U23 | P0 | 作为普通用户，我希望按空间/部门/类型浏览文件列表（含分页），以便找到目标文件 |
| U24 | P0 | 作为普通用户，我希望通过文件名关键字搜索文件，以便快速定位文件 |
| U25 | P1 | 作为普通用户，我希望重命名/移动（变更空间归属）文件，以便整理我的文件 |
| U26 | P1 | 作为文件所有者，我希望删除文件（软删除）并在一定期限内可恢复，以便防止误删造成损失 |
| U27 | P2 | 作为普通用户，我希望查看文件元信息（大小/类型/上传人/上传时间），以便判断文件是否可用 |
| U28 | P1 | 作为文件所有者/部门管理员，我希望修改文件归属（变更空间、部门或移交归属人），以便将文件移交到正确的权限范围 |

### 3.6 磁盘持久化（U29 ~ U32）

| ID | 优先级 | 用户故事 |
|---|---|---|
| U29 | P0 | 作为普通用户，我希望上传的文件物理落盘并长期保存，以便文件不因服务重启而丢失 |
| U30 | P0 | 作为系统管理员，我希望系统磁盘使用情况可被监控，以便在磁盘满前及时告警扩容 |
| U31 | P1 | 作为系统管理员，我希望支持对存储目录的配置，以便将文件存储到指定的磁盘路径 |
| U32 | P2 | 作为系统管理员，我希望支持定时备份机制，以便在灾难发生时能够恢复文件 |

### 3.7 安全与审计（U33 ~ U35）

| ID | 优先级 | 用户故事 |
|---|---|---|
| U33 | P0 | 作为系统管理员，我希望记录关键操作日志（登录/上传/下载/删除），以便安全审计与追溯 |
| U34 | P1 | 作为系统管理员，我希望敏感操作（删除、权限变更）有操作留痕，以便界定责任 |
| U35 | P2 | 作为系统用户，我希望密码以加密形式存储，以便密码不被明文泄露 |

---

## 4. 功能需求详述

### 4.1 登录鉴权（F1）

| 编号 | F1 |
|---|---|
| 功能名称 | 认证与登录 |
| 前置条件 | 用户账号已由管理员创建 |
| 主流程 | 1. 用户访问登录页，加载图片验证码（`GET /api/captcha`）→ 2. 输入用户名、验证码，前端用 RSA 公钥加密密码后提交 → 3. 后端校验验证码 → 4. 校验凭证（解密密码 + BCrypt 比对）→ 5. 通过后执行多登录踢出（同一账号旧会话失效）→ 6. 生成会话（Token，存 Redis）→ 7. 重定向至文件首页 |
| 异常流程 | 1. 验证码错误：提示"验证码错误或已过期"并刷新验证码；2. 凭证错误：提示"用户名或密码错误"，失败计数 +1；3. 连续失败 5 次：账号锁定 10 分钟，提示"账号已锁定，请 10 分钟后再试"；4. 账号被停用：提示"账号已停用，请联系管理员"；5. 同一账号新设备登录：旧会话被踢出 |
| 安全要求 | 1. **图片验证码**：生成后存 Redis（有效期 5 分钟，一次性），登录页每次加载/失败后刷新；2. **前端加密传输**：密码经 RSA 公钥加密后再传，后端私钥解密（RSA 密钥对启动时生成，公钥经 `GET /api/publicKey` 下发）；3. **失败锁定**：失败计数存 Redis，连续 5 次锁定 10 分钟（TTL 自动解除）；4. **多登录踢出**：单会话策略，新登录使旧会话（Redis 中 token）失效；5. 密码 BCrypt 加密存储；会话存 Redis 有效期 30 分钟，超时自动登出 |
| 验收标准 | 无验证码或验证码错误无法登录；抓包无明文密码；连续错 5 次锁定 10 分钟且到时自动解锁；新登录后旧会话立即失效；会话过期后访问需重新登录 |

### 4.2 用户与部门管理（F2）

| 编号 | F2 |
|---|---|
| 功能名称 | 用户与组织架构管理 |
| 主流程 | 1. 管理员维护部门树（增删改）→ 2. 管理员创建/编辑用户并分配部门与角色 → 3. 新用户获得初始密码，首次登录强制改密 |
| 权限约束 | 仅 `ADMIN` 可管理用户与部门；`DEPT_ADMIN` 可查看本部门用户 |
| 验收标准 | 新建用户可登录；停用用户立即无法登录；部门删除时其文件需先转移或禁止删除 |

### 4.3 文件上传（F3）

| 编号 | F3 |
|---|---|
| 功能名称 | 文件上传与持久化 |
| 触发方式 | 单文件选择 / 批量多选 / 拖拽上传（Ant Design Pro Upload 组件） |
| 传输机制 | 基于 `cn.chenxinjie:upload-file:1.0.0-rc.3` 组件（`upload-file-spring-boot-starter`）实现分片上传。协议：`POST /upload`（multipart，文件字段名 `file`），请求参数 `identifier`、`fileName`、`fileSize`、`chunkSize`、`chunkTotal`、`chunkIndex`、`chunkMd5`；进度查询 `GET /upload?action=progress&identifier=xxx`（据此跳过已传分片实现断点续传/秒传）；合并 `POST /upload?action=merge` 或异步 `mergeAsync` + `mergeStatus` 轮询 |
| 业务规则 | 1. 支持类型：全类型（不限制）；2. 单文件上限默认 500MB（由 `upload-file.max-file-size` 控制，落盘前即拒绝超限）；3. 分片大小默认 5MB（`upload-file.max-chunk-size` 限制单片上限）；4. 上传时必须指定空间（个人/部门/公共）；5. 同名文件自动加后缀（`xxx(1).pdf`）避免覆盖；6. 开启分片 MD5 校验（`upload-file.verify-checksum=true`） |
| 持久化逻辑 | 分片落 `upload-file.storage-dir` 分片目录；任务元数据经 `upload-file.metadata-store=redis` 存于 Redis 9（或 file/jdbc）；合并采用「临时文件 + 原子改名 + fsync」，按序合并并校验最终大小，合并后自动清理分片；合并完成的文件纳入 PathFinder 统一存储（UUID 命名 + 日期分目录） |
| 异常处理 | 网络中断后客户端查询进度从断点续传；分片 MD5 不一致（`ChecksumMismatchException`）自动重传该分片；超限/参数非法返回 `400`，配额不足返回 `507`；令牌缺失返回 `401` |
| 验收标准 | 大文件分片上传、断点续传、秒传成功且内容完整；`mergeAsync` 异步合并成功且状态轮询正确；同名不覆盖；超限文件被拦截并提示 |

### 4.4 文件下载（F4）

| 编号 | F4 |
|---|---|
| 功能名称 | 文件下载 |
| 传输机制 | 基于 `cn.chenxinjie:upload-file:1.0.0-rc.3` 完成分片上传与合并；**下载由 PathFinder 下载端点（`GET /api/file/download/{token}`，本地流式）提供**，实现与组件一致的 Range 断点续传语义：完整下载返回 `200`，携带 `Range` 头返回 `206 Partial Content`（区间不可满足返回 `416`）。组件 `GET /download?identifier=xxx` 端点保留用于未入库临时文件/联调兜底，不承载业务下载 |
| 业务规则 | 1. 仅可下载有权限的文件（下载令牌由后端签发，`identifier` 与真实磁盘路径不对外暴露）；2. 批量下载由本系统先 ZIP 打包为临时文件，再经下载端点返回；3. ZIP 打包数量上限 100 个文件/单次 |
| 验收标准 | 无权限文件下载被拒绝（403）；下载文件名正确；ZIP 可正常解压；Range 断点续传后文件内容完整（MD5 一致） |

### 4.5 文件管理与检索（F5）

| 编号 | F5 |
|---|---|
| 功能名称 | 文件列表、检索与操作 |
| 业务规则 | 1. 列表按空间/部门/文件类型过滤，采用**后端真分页**（数据库层分页，非前端全量加载）：前端传递 `pageNum/pageSize`，后端 `LIMIT/OFFSET` 查询并返回总条数；2. 搜索支持文件名模糊匹配；3. 删除采用软删除（`del_flag`），回收站保留 30 天；4. 删除/移动等操作需校验所有者或角色权限；5. 文件元信息、部门树、用户信息经 Redis 缓存，变更时主动失效 |
| 验收标准 | 真分页：翻页仅请求当前页数据、总条数准确、大数据量下仍响应正常；搜索命中准确；软删除后列表不可见、回收站可见；30 天后物理清除；缓存命中数据与库内一致 |

### 4.6 磁盘持久化与存储（F6）

| 编号 | F6 |
|---|---|
| 功能名称 | 磁盘存储与监控 |
| 业务规则 | 1. 存储根目录可配置；2. 按日期分目录落盘；3. 系统启动时校验并初始化存储目录；4. 提供存储用量统计（总容量/已用/剩余）与磁盘使用率告警（阈值默认 85%）；5. 删除文件先移入归档目录（`del`），延迟清理 |
| 验收标准 | 重启后文件仍可访问；磁盘使用率超阈值触发告警；存储目录不存在时自动创建 |

### 4.7 审计日志（F7）

| 编号 | F7 |
|---|---|
| 功能名称 | 操作审计 |
| 业务规则 | 记录：操作人、IP、User-Agent、操作类型（登录/上传/下载/删除/归属变更/改密）、目标文件、结果、时间；支持按用户/时间/操作类型筛选查询 |
| 验收标准 | 关键操作均产生日志；日志可查询与导出 |

### 4.8 大文件传输组件集成（F8）

| 编号 | F8 |
|---|---|
| 功能名称 | 大文件分片上传 / 断点续传 / 断点下载组件集成 |
| 依赖坐标 | `cn.chenxinjie:upload-file:1.0.0-rc.3`（父 POM）；本系统实际引用 `cn.chenxinjie:upload-file-spring-boot-starter:1.0.0-rc.3`，可选 `upload-file-store-redis`（Redis 任务存储） |
| 引入模块 | 1. `upload-file-spring-boot-starter`：自动配置，零编码注册 `POST /upload`、`GET /upload`（progress/merge/mergeAsync/mergeStatus）、`GET /download` 接口；2. `upload-file-store-redis`：任务元数据存储于 Redis 9（`upload-file.metadata-store=redis`） |
| HTTP API 契约 | 上传分片 `POST /upload`（multipart 字段 `file`，参数 `identifier`/`fileName`/`fileSize`/`chunkSize`/`chunkTotal`/`chunkIndex`/`chunkMd5`）→ 返回进度 JSON；进度 `GET /upload?action=progress&identifier=xxx`；同步合并 `POST /upload?action=merge&identifier=xxx`；异步合并 `POST /upload?action=mergeAsync&identifier=xxx`（`202`）+ `GET /upload?action=mergeStatus&identifier=xxx`（`NONE/PENDING/RUNNING/SUCCEEDED/FAILED`）；下载 `GET /download?identifier=xxx`（支持 `Range`，`200/206/416`） |
| 关键配置 | `storage-dir`（分片/合并根目录）、`metadata-store=redis` + `redis.*`（host/port/password/key-prefix/ttl-seconds）、`verify-checksum=true`、`max-chunk-size`、`max-file-size`、`quota.max-bytes`（全局配额）、`async-merge.enabled=true`、`cleanup.*`（过期任务/孤儿清理，可配 `use-redis-lock`）、`security.*`（可选共享令牌，本系统 v1.0 不启用，鉴权由 PathFinder 认证层完成） |
| 错误码映射 | `400` 参数非法 / 超 `max-file-size`；`401` 访问令牌缺失；`404` 文件不存在；`416` Range 不可满足；`507 Insufficient Storage` 超全局配额 |
| 集成约束 | 1. PathFinder 认证与数据权限校验位于该组件接口之前，校验通过后生成内部 `identifier`（UUID），不对外暴露真实存储路径；2. 前端上传协议（multipart 字段名与全部请求参数）必须与组件契约一致；3. 组件元数据不落入 PathFinder 业务库，上传任务状态由组件管理（Redis/File TaskStore）；4. 业务下载由 PathFinder 下载端点承载（见 F4），组件 `/download` 仅用于未入库临时文件/联调兜底 |
| 验收标准 | 集成后分片上传/断点续传/秒传/Range 下载端到端可用；配置项与组件 README 契约一致；错误码能被前端识别并友好提示 |

### 4.9 文件归属变更（F9）

| 编号 | F9 |
|---|---|
| 功能名称 | 修改文件归属（空间 / 部门 / 归属人移交） |
| 前置条件 | 用户已登录且对目标文件具备归属变更权限 |
| 主流程 | 1. 用户选择文件，点击"修改归属" → 2. 选择目标归属：空间类型（个人/部门/公共）+ 目标部门（空间为"部门"时必选） → 3. 可选"移交归属人"（选择组织内用户） → 4. 系统校验权限并预览变更后的可见范围 → 5. 确认后更新 `space_type` / `dept_id` / `owner_id` → 6. 记录归属变更审计日志 |
| 权限约束 | 1. 文件所有者：可修改归属；2. 部门管理员：可修改本部门及下级部门范围内文件的归属；3. 系统管理员：可修改全部文件归属；4. 普通用户无权操作他人文件；5. 移交归属人仅限文件所有者或部门/系统管理员 |
| 业务规则 | 1. 变更目标空间/部门必须与文件当前可见范围一致或有权限（越权目标被拒绝并提示）；2. 部门空间文件移交后 `dept_id` 同步更新，可见性即时生效；3. 归属变更不改变物理存储位置（仅更新元数据）；4. 变更操作记录审计日志（操作人、原归属、新归属、时间） |
| 异常流程 | 目标部门不存在或已停用：提示并阻止；目标空间无权限：提示"无权将文件归属到该空间/部门"；文件处于上传/合并中：提示稍后再操作 |
| 验收标准 | 归属变更成功后可见范围即时变化；越权变更被拒绝（403）并记录日志；审计日志完整可查；物理文件未发生移动 |

---

## 5. 技术方案要点

> 详细技术设计见《PathFinder 技术设计文档》（后续单独编写），本处仅作概述。

| 层面 | 方案 |
|---|---|
| 前端 | React 18 + Ant Design Pro（UmiJS 框架）+ TypeScript，按角色动态渲染侧边栏与页面 |
| 后端 | JDK 26 + Spring Boot 4.1.1 + Spring Security + Spring Data JPA + MySQL 8（生产）/ H2（开发） |
| 缓存 | Redis 9：会话/Token 存储、文件元信息缓存、部门树与用户信息缓存、登录失败计数（锁定） |
| 鉴权 | Spring Security + Redis 会话管理（单会话，多登录踢出）；图片验证码（Redis 一次性校验）；前端 RSA 公钥加密密码传输、后端 BCrypt 校验；登录失败计数落 Redis（连续 5 次锁定 10 分钟）；自定义登录成功/失败处理器 |
| 大文件传输 | 集成 `cn.chenxinjie:upload-file:1.0.0-rc.3`（`upload-file-spring-boot-starter` + `upload-file-store-redis`）实现分片上传 / 断点续传 / MD5 校验 / 异步合并 / 过期任务清理；Range 断点下载由 PathFinder 下载端点实现（语义与组件一致），接口契约见 F8/F4 |
| 数据权限 | 文件表含 `space_type`（PERSONAL/DEPT/PUBLIC）+ `dept_id` + `owner_id`（归属人）+ `creator_id`，查询时按可见性规则动态过滤；归属变更仅更新元数据不移动物理文件（见 F9） |
| 列表分页 | **真分页**：后端 Spring Data JPA `Pageable` + 数据库 `LIMIT/OFFSET` 分页（不依赖前端内存分页），返回当前页数据与 `total`；`pageNum/pageSize` 参数化并限制 `pageSize` 上限（默认 20，最大 100） |
| 文件存储 | 本地磁盘持久化（`upload-file.storage-dir` 落分片与合并文件 + `storage.root` 归档业务文件），UUID 文件名 + 日期分目录 + 软删除归档 |
| 数据模型 | 核心表：`sys_user`、`sys_dept`、`sys_role`、`file_info`、`operation_log`、`file_recycle_bin`；上传任务元数据由组件管理（Redis `TaskStore`，key 前缀 `upload:task:`） |
| 部署 | Docker 部署（前端 nginx:alpine 镜像 + 后端镜像 + Redis 9 + MySQL），文件目录以 volume 挂载持久化 |

### 5.1 代码工程目录结构

```
path-finder/
├── docs/                            # 文档目录
│   ├── PRD-PathFinder-v1.0.0.md     #   产品需求文档
│   ├── design/                      #   技术设计文档（TSDD/API 契约等）
│   └── assets/                      #   文档图片/附件
├── frontend/                        # 前端代码目录（React 18 + Ant Design Pro / UmiJS + TS）
│   ├── src/
│   │   ├── pages/                   #   页面：login / fileList / user / dept / log…
│   │   ├── components/              #   通用业务组件（文件列表、上传弹窗等）
│   │   ├── services/                #   后端 API 请求封装
│   │   ├── hooks/                   #   自定义 hooks
│   │   ├── utils/                   #   工具函数（分页、格式化等）
│   │   ├── access.ts                #   角色/权限控制（按角色渲染）
│   │   ├── config.ts
│   │   └── app.tsx
│   ├── tests/                       # 前端自动化测试代码目录
│   │   ├── unit/                    #   Jest 单元测试（组件/工具）
│   │   └── e2e/                     #   E2E 测试（Playwright：登录、上传、断点续传）
│   ├── package.json
│   └── config/                      #   UmiJS 配置（routes、proxy、theme）
├── server/                          # 后端代码目录（Spring Boot Maven 单模块）
│   ├── src/main/
│   │   ├── java/com/pathfinder/
│   │   │   ├── config/              #   配置类（Security、Web、Redis、上传组件）
│   │   │   ├── controller/          #   控制器（auth / user / dept / file / log）
│   │   │   ├── service/             #   业务服务层
│   │   │   ├── repository/          #   数据访问层（JPA Repository）
│   │   │   ├── entity/              #   实体（User / Dept / FileInfo / OperationLog…）
│   │   │   ├── security/            #   认证授权（过滤器、处理器、UserDetailsService）
│   │   │   └── util/                #   工具类
│   │   └── resources/
│   │       ├── application.yml      #   通用配置（多环境 profile 覆盖）
│   │       └── db/                  #   SQL 初始化脚本
│   └── src/test/java/com/pathfinder/   # 后端自动化测试代码目录（JUnit 5）
│       ├── controller/              #   接口/集成测试
│       ├── service/                 #   服务单元测试
│       └── security/                #   鉴权与数据权限测试
├── docker/                          # Docker 编排目录（docker-compose、nginx.conf、Dockerfile）
├── scripts/                         # 运维脚本（备份、部署、健康检查）
└── .github/                         # CI/CD 配置（构建、测试覆盖率门禁）
```

| 目录 | 说明 | 测试代码位置 |
|---|---|---|
| `docs/` | 文档目录：PRD、技术设计、API 契约、图片附件 | — |
| `frontend/` | 前端代码目录：React + Ant Design Pro（UmiJS），`src/` 放业务源码，`tests/` 放自动化测试 | `frontend/tests/unit`（Jest）、`frontend/tests/e2e`（Playwright） |
| `server/` | 后端代码目录：Spring Boot Maven 单模块，`src/main` 放业务源码，`src/test` 放自动化测试 | `server/src/test/java/com/pathfinder/`（JUnit 5） |
| `docker/` | 部署编排：前端 `nginx:alpine`、后端、Redis 9、MySQL 的镜像与 compose 配置 | — |
| `scripts/` | 运维脚本：备份（存储目录 + MySQL + Redis）、部署、健康检查 | — |
| `.github/` | CI/CD：构建、测试执行与覆盖率门禁 | — |

> 覆盖率统计基线：后端以 `server/src/test` 为准（JaCoCo），前端以 `frontend/tests/unit` 为准（Jest coverage）。

### 5.2 代码质量约束

| 类别 | 约束 |
|---|---|
| 命名规范 | 后端：包名全小写、类名大驼峰（`PascalCase`）、方法/变量小驼峰（`camelCase`）、常量全大写；前端：组件文件 `PascalCase`、普通变量/函数 `camelCase`、常量 `UPPER_SNAKE`；禁止拼音命名，一律语义化英文 |
| 注释规范 | 1. 公共类/公共方法必须编写 Javadoc / TSDoc（说明用途、参数、返回值、异常）；2. 复杂业务逻辑（算法、权限判定、归属变更等）必须写行内注释说明意图；3. 魔法值/常量需注释含义或提取为具名常量；4. 遗留问题用 `TODO` / `FIXME` 标注并说明原因；5. 禁止无意义注释（复述代码的废话注释）、禁止注释掉的死代码 |
| 后端规范 | 严格 Controller → Service → Repository 分层，禁止跨层调用；事务只加在 Service 层（`@Transactional` 明确边界）；日志用 SLF4J 占位符（`log.info("upload id={}", id)`）不拼字符串；异常统一由全局异常处理器兜底，不吞异常 |
| 前端规范 | TypeScript `strict` 模式开启，禁止 `any`（特殊场景需注释说明）；组件单一职责；hooks 遵循 React 官方规范；样式使用 Ant Design Pro 主题变量，禁止散落魔法色值 |
| 静态检查 | 后端：Checkstyle + SpotBugs 门禁（违反即构建失败）；前端：ESLint（`@typescript-eslint` 严格规则集）+ Prettier（格式统一，`format` 校验进 CI） |
| 提交规范 | 采用 Conventional Commits（`feat` / `fix` / `docs` / `refactor` / `test` / `chore` + scope）；提交信息描述变更意图；禁止将调试代码、无关注释、临时文件混入提交 |
| 评审门禁 | 每次变更走 Code Review：静态检查通过、测试覆盖达标（见非功能需求）、无 `FIXME` 残留后方可合并 |
| 可观测性 | 关键路径（登录、上传合并、归属变更）必须输出结构化日志（操作人/目标/结果），满足审计要求（见 F7） |

---

## 6. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 列表接口响应 < 500ms（热数据走 Redis 缓存，真分页只查询当前页）；并发上传 50 文件无阻塞；万级文件真分页翻页响应正常；千级文件搜索 < 3s；缓存命中率 ≥ 85% |
| 容量 | 单文件 ≤ 500MB（`upload-file.max-file-size` 控制，落盘前拒绝）；分片大小默认 5MB（`max-chunk-size` 限制）；全局配额 `quota.max-bytes` 可选开启；初始存储容量 500GB（按磁盘实际决定） |
| 安全 | 图片验证码（Redis 一次性校验）；前端 RSA 加密传输密码 + 后端 BCrypt 存储；登录失败 5 次锁定 10 分钟（Redis 计数）；多登录踢出（单会话）；会话超时；越权访问拦截；上传文件类型无脚本执行风险（仅静态存储，不服务渲染） |
| 可用性 | 单机部署可用性 ≥ 99%；Redis/MySQL 故障不影响已落盘文件读取；磁盘满自动告警，不丢数据 |
| 兼容性 | 支持 Chrome/Edge/Firefox 最新两个大版本；`cn.chenxinjie:upload-file` 产物为 JDK 8 字节码，与 JDK 26 运行兼容性待集成验证 |
| 备份 | 提供存储目录 + MySQL + Redis（AOF/RDB，含 `upload:task:` 上传任务元数据）的备份脚本（P2） |
| 测试覆盖率 | 后端：JUnit 5 单元/集成测试，整体行覆盖率 ≥ 80%，核心模块（鉴权、数据权限、文件上传下载、审计日志）≥ 85%，增量代码覆盖率 ≥ 90%；前端：Jest + React Testing Library，核心交互（登录鉴权、上传流程、权限渲染、列表检索）语句覆盖率 ≥ 70%，新增组件 ≥ 80% |

---

## 7. 数据权限示例场景

| 场景 | 可见范围 |
|---|---|
| 员工 A（研发部）上传文件到"个人空间" | 仅 A 可见 |
| 员工 A 上传到"研发部空间" | 研发部及下级部门所有成员可见 |
| 部门管理员查看"研发部空间" | 研发部 + 全部下级部门文件可见 |
| 系统管理员 | 全部空间、全部部门文件可见 |
| 员工 A 尝试下载公共空间文件 | 允许（公共空间全组织可见） |
| 员工 A 尝试下载财务部空间文件 | 拒绝（403），并记录审计日志 |

---

## 8. 版本规划

| 版本 | 时间 | 范围 |
|---|---|---|
| v1.0.0（当前） | 2026 Q3 | 登录鉴权、用户/部门管理、上传下载、文件管理检索、磁盘持久化、审计日志 |
| v1.1.0 | 2026 Q4 | 在线预览（图片/PDF/Office）、回收站增强、存储容量管理 |
| v1.2.0 | 2027 Q1 | 在线编辑协作、分享链接（含有效期与密码） |
| v1.3.0 | 2027 Q2 | 全文检索、OCR 识别、AI 智能分类标签 |

---

## 9. 成功指标

| 指标 | 目标值 | 统计来源 |
|---|---|---|
| 组织内文件统一入库率 | 上线 3 个月 ≥ 80% | 文件表统计 |
| 越权访问拦截率 | 100% | 审计日志 |
| 登录成功率 | ≥ 99% | 登录日志 |
| 用户周活跃使用率 | ≥ 70% | 操作日志 |
| 平均检索耗时 | < 3s | 性能监控 |
| 磁盘告警响应 | 告警后 24h 内扩容 | 运维记录 |

---

## 10. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 磁盘空间被恶意填满 | 服务不可用 | 上传大小限制 + 用量告警 + 配额（v1.1 支持用户配额） |
| 员工误删重要文件 | 数据丢失 | 软删除 + 回收站 30 天 + 定期备份 |
| 离职人员带走文件 | 资产流失 | 账号停用即时生效 + 操作审计 |
| 存储目录损坏 | 文件不可恢复 | 目录分级持久化 + 备份脚本 |
| 单点故障 | 服务中断 | v1.0 单机部署可接受，v1.1 引入双机容灾评估 |
| Redis 故障 | 会话丢失、缓存失效、断点续传状态（`upload:task:`）丢失 | Redis 开启持久化；故障时降级为 DB 直查与会话本地兜底，已落盘文件不受影响；关键上传任务可切换 `metadata-store=file` 兜底 |
| 第三方组件兼容性 | `upload-file-spring-boot-starter` 官方面向 Spring Boot 2.x，与 Spring Boot 4.1.1 / Redis 9 的集成存在适配风险 | 集成阶段先行 POC 验证；如 starter 不适配，降级为直接引用 `upload-file-servlet`（Servlet 3.0+ 接入）或 `upload-file-core` 手动装配，接口契约不变 |
